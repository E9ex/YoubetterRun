using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundTile : MonoBehaviour
{
  /*  private GroundSpawner GroundSpawner;
    void Start()
    {
        GroundSpawner = GameObject.FindObjectOfType<GroundSpawner>();
    }

    private void OnTriggerExit(Collider other)
    {
        GroundSpawner.spawnTile();
        Destroy(gameObject,2f);
    }

    void Update()
    {
        
    }*///at ground a path 'e.
  
  
  /*
   *
   * public GameObject[] obstaclePrefabs;
public Transform[] spawnPoints;

void SpawnObstacle() {
    int obstacleSpawnIndex = Random.Range(0, spawnPoints.Length);
    Transform spawnPoint = spawnPoints[obstacleSpawnIndex];

    int obstacleIndex = Random.Range(0, obstaclePrefabs.Length);
    GameObject obstaclePrefab = obstaclePrefabs[obstacleIndex];

    Instantiate(obstaclePrefab, spawnPoint.position, Quaternion.identity);
}

   */
}
